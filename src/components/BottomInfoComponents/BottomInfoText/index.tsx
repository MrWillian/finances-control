import React from 'react';

import { Text } from './styles';

const BottomInfoText: React.FC = ({ children }) => {
  return (
    <Text>
      {children}
    </Text>
  );
}

export default BottomInfoText;
